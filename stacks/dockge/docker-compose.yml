services:

  dockge:
    image: louislam/dockge:1.5.0
    container_name: dockge
    restart: unless-stopped

    ports:
      - "5001:5001"

    environment:
      - DOCKGE_STACKS_DIR=/workspace/stacks
      - DOCKGE_ENABLE_CONSOLE=true

    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${ROOT}/runtime/dockge:/app/data
      - ${ROOT}:/workspace